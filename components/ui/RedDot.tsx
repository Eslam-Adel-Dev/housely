import React from "react";
import { View } from "react-native";

const RedDot = () => {
  return (
    <View className="absolute top-0 right-0 bg-red-500 rounded-full size-3 z-10" />
  );
};

export default RedDot;
